import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.QueratorShuttle.app',
  appName: 'Querator Shuttle',
  webDir: 'dist'
};

export default config;
